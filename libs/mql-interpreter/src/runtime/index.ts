export { execute, callFunction, instantiate, callMethod } from "./runtime";
export type {
  RuntimeState,
  ExecutionContext,
  RuntimeFunctionParameter,
  RuntimeClassField,
  RuntimeClassMethod,
  ProgramType,
} from "./types";
